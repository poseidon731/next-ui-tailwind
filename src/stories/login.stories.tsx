import React from "next/app";
import "./tailwind.css";

const stories = {
  title: `Sample/Login`,
};

export default stories;

export const Index = () => (
  <main>
    <section className="relative w-full h-full py-40 min-h-screen"></section>
  </main>
);
